version: '3'

tasks:
  default:   
    cmds:
      - task -l
    #silent: true
  run:   
    desc: runs app
    cmds:
      - go run main.go
    #silent: true
  build: 
    desc: build the this dope application
    deps:
      - clean
    cmds:
      - go build -o dist/ main.go
    #silent: true
  clean: 
    desc: remove dist files
    cmds:
      - mkdir -p dist/
      - rm -rf dist/*
    #internal: true
